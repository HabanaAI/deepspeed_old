{
  "steps_per_print": 1,
  "train_batch_size": 16,
  "train_micro_batch_size_per_gpu": 16,
  "scheduler": {
    "type": "WarmupDecayLR",
    "params": {
      "warmup_min_lr": 0,
      "warmup_max_lr": 1e-4,
      "warmup_num_steps": 10000,
      "total_num_steps" : 2000000,
      "warmup_type": "linear"
    }
  }
}